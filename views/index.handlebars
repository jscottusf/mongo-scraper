<div class="container">
    {{> header}}
    <div class="grid-container">
        {{#each Article }}
        {{> articles}}
        {{/each}}
    </div>
</div>
{{> note}}
<script src="/js/index.js"></script>